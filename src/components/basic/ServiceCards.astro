---
const services = [
  {
    name: 'Plumbing',
    desc: 'Repairs, installations, and emergency service for residential and commercial properties.',
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M4 14h3a2 2 0 012 2v4H4v-6zM15 3v4a2 2 0 01-2 2H9a2 2 0 01-2-2V3M9 7v6M20 14h-3a2 2 0 00-2 2v4h5v-6z"/></svg>`
  },
  {
    name: 'Electrical',
    desc: 'Panel upgrades, rewiring, lighting, and code-compliant installations.',
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M13 2L3 14h9l-1 8 10-12h-9l1-8z"/></svg>`
  },
  {
    name: 'HVAC',
    desc: 'Heating, cooling, and ventilation systems — install, repair, and maintain.',
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/><circle cx="12" cy="12" r="3"/></svg>`
  },
  {
    name: 'Roofing',
    desc: 'Inspections, repairs, replacements, and storm damage restoration.',
    icon: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 21h18M3 21V10l9-7 9 7v11M9 21v-6h6v6"/></svg>`
  }
];
---

<section class="services-section" id="services">
  <h2 class="section-title">Our Services</h2>
  <p class="section-subtitle">Licensed professionals delivering quality work across every trade.</p>

  <div class="services-grid">
    {services.map((s, i) => (
      <div class="service-card" style={`animation-delay: ${i * 0.1}s`}>
        <div class="service-icon" set:html={s.icon} />
        <h3>{s.name}</h3>
        <p>{s.desc}</p>
        <a href={`#contact`} class="service-link">Get a Quote &rarr;</a>
      </div>
    ))}
  </div>
</section>

<style>
  .services-section {
    padding: 5rem 2rem;
    background: var(--bg-main);
    text-align: center;
  }

  .section-title {
    font-size: 2.5rem;
    color: var(--dark);
    font-weight: 700;
    margin-bottom: 0.75rem;
  }

  .section-subtitle {
    font-size: 1.15rem;
    color: var(--text-light);
    margin-bottom: 3.5rem;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    max-width: 1200px;
    margin: 0 auto;
  }

  .service-card {
    background: var(--bg-main);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 2.5rem 2rem;
    text-align: center;
    transition: all 0.3s ease;
    opacity: 0;
    animation: fadeUp 0.5s ease-out forwards;
  }

  .service-card:hover {
    transform: translateY(-6px);
    border-color: var(--accent);
    box-shadow: 0 20px 40px rgba(var(--accent-rgb), 0.12);
  }

  .service-icon {
    width: 48px;
    height: 48px;
    margin: 0 auto 1.5rem;
    color: var(--accent);
  }

  .service-icon :global(svg) {
    width: 100%;
    height: 100%;
  }

  .service-card h3 {
    font-size: 1.3rem;
    color: var(--dark);
    font-weight: 700;
    margin-bottom: 0.75rem;
  }

  .service-card p {
    font-size: 0.95rem;
    color: var(--text);
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .service-link {
    color: var(--accent);
    font-weight: 600;
    text-decoration: none;
    font-size: 0.95rem;
    transition: color 0.2s;
  }

  .service-link:hover {
    color: var(--accent-hover);
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (max-width: 1024px) {
    .services-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 640px) {
    .services-grid { grid-template-columns: 1fr; }
    .section-title { font-size: 2rem; }
  }
</style>
